API_PORT=4000
DATABASE_URL=postgresql://electrotech:electrotech@localhost:5432/electrotech

# Публичный origin API (как в браузере), со схемой, без пути — например https://electro-tech-api.inverseai.ru
# Для логов AdminJS и trust proxy за HTTPS. Можно не задавать локально; в проде совпадает с NEXT_PUBLIC_API_URL фронта.
# PUBLIC_API_URL=https://electro-tech-api.inverseai.ru
# Явно выключить доверие к X-Forwarded-* (по умолчанию: production = включено)
# TRUST_PROXY=false

# CORS: браузерный origin фронтов (через запятую)
WEB_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001

# В production оставьте synchronize выключенным (NODE_ENV=production по умолчанию)
# TYPEORM_SYNC=false

# Лог SQL в консоль (опционально)
# TYPEORM_LOGGING=true

# AdminJS (http://localhost:4000/admin)
ADMIN_EMAIL=admin@local.dev
ADMIN_PASSWORD=admin123
# Лучше 32+ символов (в docker-compose.vps уже длинный дефолт). Короткие значения на API усиливаются через SHA-256.
ADMIN_SESSION_SECRET=dev-admin-session-secret-at-least-32-chars-long
# NODE_ENV=production: без кастомных React-компонентов AdminJS лучше true (не гоняем Rollup при старте).
ADMIN_JS_SKIP_BUNDLE=true

# Пересид каталога при старте (только dev): очищает таблицы и заливает моки
CATALOG_FORCE_SEED=true

# Демо на VPS (NODE_ENV=production): очистить каталог и снова залить моки. Опасно на бою.
# CATALOG_DEMO_RESEED=true
