# Next.js подхватывает этот файл в `next dev` (можно коммитить).
# URL бэкенда для SSR и для режима с реальным API (см. README).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Отключить телеметрию Next.js в dev
NEXT_TELEMETRY_DISABLED=1
