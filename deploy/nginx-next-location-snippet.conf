# Вставьте внутрь `location / { ... }` прокси на Next (оба server: web и при необходимости supplier).
# Снимает обрезание потокового ответа App Router и таймауты на медленных ответах.

proxy_http_version 1.1;
proxy_buffering off;
proxy_read_timeout 300s;
proxy_send_timeout 300s;
